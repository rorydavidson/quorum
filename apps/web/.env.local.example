# Copy to .env.local — BFF_URL is server-only (no NEXT_PUBLIC_ prefix)

# BFF URL — used server-side only (never exposed to browser)
BFF_URL=http://localhost:3001

# Public config
NEXT_PUBLIC_APP_NAME=Quorum
NEXT_PUBLIC_KEYCLOAK_REALM=snomed
